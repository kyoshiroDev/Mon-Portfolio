{
  "name": "forgedigitalweb",
  "version": "1.0.0",
  "description": "Mon portfolio",
    "author": "Tahir Grégory",
  "license": "ISC",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
